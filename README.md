```python
from setuptools import setup

setup(
    name="omar-zoloev",
    version="1.3.7",
    requires=["pytorch", "aten"],
    extras=["cuda, c++"],
    description="nlp, low-level, research"
)

```
